server:
  servlet:
    context-path: /Identity
  port: 8081
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/Identity_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
