server:
  servlet:
    context-path: /Identity
  port: 8080
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/User_Account"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
